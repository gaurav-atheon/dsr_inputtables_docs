version: 2

models:
  - name: dim_org_calendar
    description: '{{ doc("dim_org_calendar") }}'
    columns:
      - name: organisation_id
        tests:
          - relationships:
              to: ref('dim_organisation')
              field: organisation_id
  - name: dim_date
    description: '{{ doc("dim_date") }}'