version: 2
models:
  - name: dim_location
    columns:
      - name: organisation_id
        tests:
          - relationships:
              to: ref('dim_organisation')
              field: organisation_id
  - name: dim_location_group
    columns:
      - name: subject_organisation_id
        tests:
          - relationships:
              to: ref('dim_organisation')
              field: organisation_id
      - name: creator_organisation_id
        tests:
          - relationships:
              to: ref('dim_organisation')
              field: organisation_id
  - name: dim_location_parentage
    columns:
      - name: subject_organisation_id
        tests:
          - relationships:
              to: ref('dim_organisation')
              field: organisation_id
      - name: creator_organisation_id
        tests:
          - relationships:
              to: ref('dim_organisation')
              field: organisation_id
      - name: parent_organisation_id
        tests:
          - relationships:
              to: ref('dim_organisation')
              field: organisation_id