version: 2

models:
  - name: dim_logisticitem
    description: '{{ doc("dim_logisticitem") }}'
    columns:
      - name: organisation_id
        tests:
          - relationships:
              to: ref('dim_organisation')
              field: organisation_id
      - name: product_id
        tests:
          - relationships:
              to: ref('dim_product')
              field: product_id
  - name: dim_logisticitem_mapping
    description: '{{ doc("dim_logisticitem_mapping") }}'
    columns:
      - name: creator_organisation_id
        tests:
          - relationships:
              to: ref('dim_organisation')
              field: organisation_id
      - name: logisticitem_id
        tests:
          - relationships:
              to: ref('dim_logisticitem')
              field: logisticitem_id
  - name: dim_tu
    description: '{{ doc("dim_tu") }}'
  - name: int_all_ghost_logisticitem
    description: '{{ doc("int_all_ghost_logisticitem") }}'
