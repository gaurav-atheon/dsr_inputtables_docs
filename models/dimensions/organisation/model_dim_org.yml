version: 2

models:
  - name: utl_organisation_hierarchy
    description: '{{ doc("utl_organisation_hierarchy") }}'
  - name: dim_organisation_mapping
    columns:
      - name: organisation_id
        tests:
          - relationships:
              to: ref('dim_organisation')
              field: organisation_id
      - name: origin_organisation_id
        tests:
          - relationships:
              to: ref('dim_organisation')
              field: organisation_id
  - name: dim_organisation_parentage
    description: '{{ doc("dim_organisation_parentage") }}'
    columns:
      - name: creator_organisation_id
        tests:
          - relationships:
              to: ref('dim_organisation')
              field: organisation_id
      - name: subject_organisation_id
        tests:
          - relationships:
              to: ref('dim_organisation')
              field: organisation_id
      - name: parent_organisation_id
        tests:
          - relationships:
              to: ref('dim_organisation')
              field: organisation_id
  - name: dim_organisation
    description: '{{ doc("dim_organisation") }}'
  - name: int_all_ghost_organisation
    description: '{{ doc("int_all_ghost_organisation") }}'
  - name: utl_master_organisation
    description: '{{ doc("utl_master_organisation") }}'
  - name: utl_organisation_descendants
    description: '{{ doc("utl_organisation_descendants") }}'
  - name: utl_organisation_parents
    description: '{{ doc("utl_organisation_parents") }}'
  - name: utl_retailer_organisations
    description: '{{ doc("utl_retailer_organisations") }}'
  - name: utl_source_organisations
    description: '{{ doc("utl_source_organisations") }}'